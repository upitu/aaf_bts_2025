import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: { primary: { main: '#556dc5ff' }, background: { default: '#f5f5f5' } },
    typography: { fontFamily: 'Inter, sans-serif' },
});

export default theme;